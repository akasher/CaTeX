name: example
description: An example of how the CaTeX package can be used in a Flutter app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  catex:
    path: ../

dev_dependencies:
  lint: ^1.2.0 # https://pub.dev/packages/lint

flutter:
  uses-material-design: true
